#!/usr/bin/env bash
set -euo pipefail

echo "Deploy GREEN placeholder. Connect to ECS/Fargate once Terraform and ECS service names are available."
echo "Expected service: ${APP_SERVICE_GREEN:-app-green}"
